# tailscale-proxmox.conf
# Edit these values if your setup changes.

PVE_HOSTNAME="pve"
PVE_TAILSCALE_IP="100.112.238.38"
PVE_MAGICDNS="pve.tailc2b3f5.ts.net"
PVE_WEB_PORT="8006"

# Set to true only if you want Tailscale SSH enabled on Proxmox.
ENABLE_TAILSCALE_SSH="True"

# Safer default: do not advertise Proxmox as an exit node.
ADVERTISE_EXIT_NODE="false"

# Optional subnet router. Leave empty unless you want access to your whole LAN.
# Example: LAN_ROUTES="192.168.1.0/24"
LAN_ROUTES=""
